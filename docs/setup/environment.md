# Setup & Environment

## Prerequisites

- Flutter SDK (stable)
- Android Studio / Xcode

## Commands

```bash
flutter pub get
flutter gen-l10n
flutter run
```

## Notes

- Localization ARB files are in `lib/l10n/`.
- `flutter: generate: true` is required in `pubspec.yaml`.
